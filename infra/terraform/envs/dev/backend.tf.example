# Copy this file to backend.tf only after choosing a real Terraform state bucket.
# Do not commit backend.tf if it contains account-specific values.
#
# terraform {
#   backend "s3" {
#     bucket         = "replace-with-your-terraform-state-bucket"
#     key            = "enterprise-ai-agent-reference/dev/terraform.tfstate"
#     region         = "us-east-1"
#     dynamodb_table = "replace-with-your-lock-table"
#     encrypt        = true
#   }
# }
